import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig({
  preset: "vercel",
  tanstackStart: {
    // You can add TanStack Start specific options here if needed
  },
});
